# ── Required ──────────────────────────────────────────────
anthropic>=0.40.0
python-dotenv>=1.0.0

# ── Web search (needed for Security News Monitoring) ──────
# Sign up at https://tavily.com for a free API key
# Then: export TAVILY_API_KEY=tvly-...
requests>=2.31.0

# ── Google Workspace integration (Gmail + Google Docs) ────
# Optional — only needed for report generation and email delivery
# Setup: download OAuth credentials.json from Google Cloud Console
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.1.0
